#ifndef ELECTION_HPP
#define ELECTION_HPP

#include <vector>
#include <cmath>
#include <Network.hpp>

namespace election {
    int election(int n);
    int election(int n, int u);
}

#endif /* ELECTION_HPP */
